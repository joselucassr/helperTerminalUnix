@title ~$ comando &

# Basicamente
Executa o comando em segundo plano.

# Exemplos:
@CodeArea
// O slack será instalado em segundo plano
@mark("~$") sudo snap install slack --classic @mark("&")
@EndCodeArea

# Saiba Mais:

@groupExtLinks(ampersand)