@title ~$ ls -a

# Basicamente
Lista todos os arquivos e diretórios.

# Variações

@group(ls)

# Exemplos:
@CodeArea
// Listar todos os diretórios e arquivos de "estudante" dentro do diretório "trybe"
@mark("~$ ls -a") /trybe/estudante
@EndCodeArea

# Saiba Mais:

@groupExtLinks(ls)